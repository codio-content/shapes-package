from setuptools import setup

setup(
   name='shapes',
   version='1.0',
   description='An example package',
   author='Calvin & Hobbes',
   author_email='calvin@hobbes.com',
   packages=['shapes'],  
   install_requires=[],
)
